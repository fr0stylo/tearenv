{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tearenv.fullname" . }}-metrics
  labels:
    {{- include "tearenv.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "tearenv.selectorLabels" . | nindent 4 }}
{{- end }}
