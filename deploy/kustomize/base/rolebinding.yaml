apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: tearenvd-scaler
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: tearenvd-scaler
subjects:
  - kind: ServiceAccount
    name: tearenvd
