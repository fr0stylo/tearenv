apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tearenv-system

resources:
  - ../../base
  - namespace.yaml

secretGenerator:
  - name: tearenvd-bootstrap
    files:
      - users.json

images:
  - name: ghcr.io/fr0stylo/tearenv
    newTag: 0.2.0
